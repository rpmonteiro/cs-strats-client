import React, { PropTypes } from 'react';

PlayerMarkerPreview.propTypes = {
  
};

export default function PlayerMarkerPreview() {
  
  return (
    <div className="player-marker-preview">

    </div>
  );
}
